//⚔️ Day 03：實戰演武場 —— 函式吟唱基礎練習 ⚔️
// 任務說明：請打開瀏覽器的控制台 (Console)，依照指示操作咒語。

// ==========================================
// 練習一：極速吟唱變身 (Arrow Function)
// ==========================================
/* 
   任務：將下方的「傳統函式」改寫為「箭頭函式」。
   只要能成功傳回傷害數值就算達成！

   function attack(power) {
       return power * 2;
   }
*/

// ✍️ 請在下方改寫為箭頭函式：
// const attack = ...


// ==========================================
// 練習二：時空錯置的魔咒 (Hoisting)
// ==========================================
/* 
   任務：下方的咒語因為「吟唱順序」出錯了（ReferenceError）。
   請試著將 const 宣告方式，改回「傳統吟唱 (function xxx)」，
   讓咒語在定義前就能成功發動。
*/

// console.log(checkStatus()); // 🎯 目標：解除註解後不報錯，並印出狀態

const checkStatus = function() {
    return "🛡️ 勇者狀態：體力充沛！";
};


// ==========================================
// 練習三：無限背包的觀察 (...rest)
// ==========================================
/* 
   任務：我們想一次丟進很多裝備。
   請寫一個箭頭函式 `showBag`，使用其餘參數 `(...items)`，
   並且在裡面寫一行 `console.log(items)` 就好。
   
   🎯 執行 showBag("聖劍", "草藥", "盾牌") 看看印出來的是什麼。
*/

// ✍️ 作答區：


// ==========================================
// 練習四：勇者辛梅爾的圓括號秘術 (Implicit Object Return)
// ==========================================
/* 
   任務：請寫一個名為 `getHero` 的箭頭函式。
   1. 接受一個參數 `name`。
   2. 使用一行程式碼（不寫 return）回傳物件：{ heroName: name }。
   
   🎯 提示：別忘了為了讓物件不消失，需要加上防震氣泡 ( ) 哦！
*/

// ✍️ 作答區：


// ==========================================
// 演武場結束！恭喜你完成今天的修煉。
// ==========================================