// ⚔️ Day 03：實戰演武場 —— 函式吟唱基礎練習 ⚔️
// 導師：新夥伴，恭喜你開始學習封印魔法！掌握「吟唱方式」，你的程式碼就會像詩一樣優雅。
// 任務說明：請按下 CodePen 頁面左下角的「Console」按鈕（或打開瀏覽器控制台），觀察你的咒語是否生效。

// ==========================================
// 練習一：極速吟唱變身 (Arrow Function)
// ==========================================
// ⚔️ 任務情境：
// 你有一段厚重的「傳統函式」用來計算攻擊力，請將它精煉為現代勇者的「箭頭函式」。
//
// 📋 任務規範：
// 1. 將下方的 `function attack` 改寫為箭頭函式。
// 2. 挑戰目標：嘗試只用「一行程式碼 (Implicit Return)」完成它。

// 傳統吟唱：
function attack(power) {
  return power * 2;
}

// ✍️ 實作區（改寫為箭頭函式）：


// ==========================================
// 練習二：時空錯置的魔咒 (Hoisting)
// ==========================================
// ⚔️ 任務情境：
// 觀察以下程式碼。下方的咒語因為「吟唱順序」出錯了（引發 ReferenceError）。
// 
// 📋 任務規範：
// 1. 將 `const checkStatus = function()` 宣告方式，改回「傳統吟唱 (function xxx)」。
// 2. 驗證是否能在定義之前就成功噴出結果。

// console.log(checkStatus()); // 🎯 修正後解除註解，觀察是否成功執行

const checkStatus = function() {
    return "🛡️ 勇者狀態：體力充沛！";
};


// ==========================================
// 練習三：無限口袋的觀察 (...rest)
// ==========================================
// ⚔️ 任務情境：
// 在箭頭函式中，我們不再使用神祕的 arguments 背包。請練習使用現代的其餘參數。
// 
// 📋 任務規範：
// 1. 寫一個箭頭函式 `showBag`，使用其餘參數 `(...items)`。
// 2. 在裡面執行 `console.log(items)` 即可。
// 🎯 執行：`showBag("聖劍", "草藥", "盾牌")` 看看印出來的是什麼。

// ✍️ 實作區：


// ==========================================
// 練習四：勇者辛梅爾的圓括號秘術 (Implicit Object Return)
// ==========================================
// ⚔️ 任務情境：
// 當你要在箭頭函式中，回傳一個裝滿資料的「寶箱（物件）」時，必須使用特防護罩。
// 
// 📋 任務規範：
// 1. 寫一個名為 `getHero` 的箭頭函式。
// 2. 接受一個參數 `name`。
// 3. 使用一行程式碼（不寫 return）回傳物件：`{ heroName: name }`。
// 💡 提示：記得為了讓大括號不被誤認，外面需要加一個 ( ) 哦！

// ✍️ 實作區：


// ==========================================
// 💡 導師提示：
// 如果你發現箭頭函式讓你的代碼變得既簡潔又強大，代表你已掌握現代魔法的真諦。
//
// 演武場結束！恭喜你打好函式吟唱地基。
// ==========================================