// ⚔️ Day 02：實戰演武場 —— 標籤貼紙與作用域 ⚔️
// 導師：新夥伴，既然學會了「正確貼標籤」，就來演武場驗證你的眼力吧！
// 任務說明：請打開瀏覽器 Console 面板，練習觀察「變數宣告」與「作用域」的差異。

// ==========================================
// 練習一：標籤的分身與更換 (const vs let)
// ==========================================
// ⚔️ 任務情境：
// 你有兩樣物資，一個是不可更換的「稱號」，另一個是會變動的「金幣」。
// 
// 📋 任務規範：
// 1. 使用 `const` 宣告 `heroTitle`，內容為 "勇者"。
// 2. 使用 `let` 宣告 `goldCount`，初始內容為 100。
// 3. 嘗試把 `goldCount` 重新賦值為 500。
// 4. 使用 `console.log` 印出這兩樣資料。

// ✍️ 實作區：


// ==========================================
// 練習二：隱形牆壁的挑戰 (Block Scope)
// ==========================================
// ⚔️ 任務情境：
// 觀察以下程式碼。隱形房間 `{}` 會如何鎖住標籤？
// 
// 📋 任務規範：
// 1. 在 if 區塊內使用 `let` 宣告 `secretMap`。
// 2. 在 if 區塊外嘗試 `console.log(secretMap)`。
// 💡 提示：根據公會規範，這應該會噴出「ReferenceError」，這才是正確的守衛機制！

if (true) {
    // ✍️ 實作區（宣告標籤）：
}

// ✍️ 實作區（嘗試在外面印出）：


// ==========================================
// 練習三：精英挑戰 —— 型別鑑定術 (typeof)
// ==========================================
// ⚔️ 任務情境：
// 身為勇者，你必須一眼看穿物資的本質，避免把「字串 10」當成「數字 10」。
// 
// 📋 任務規範：
// 1. 對以下三樣物資進行 `typeof` 鑑定，並用 `console.log` 印出型別。
// 2. 特別觀察 `null` 的鑑定結果，並思考導師說的「遠古 Bug」。

const gold = 100;
const greeting = "Hello";
const inventory = null;

// ✍️ 實作區：


// ==========================================
// 💡 導師提示：
// 如果你發現變數「出了房間就抓不到」，或是「焊死標籤不能改」，代表你已掌握核心。
//
// 演武場結束！恭喜你打好變數地基。
// ==========================================