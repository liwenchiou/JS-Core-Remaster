// ⚔️ Day 12：【邏輯開關】&&、|| 與 ?? 的短路判斷 ⚔️

// 🛡️ 冒險者，歡迎來到「邏輯開關」演武場！
// 在這裡，我們要學習「不囉唆」的寫法。
// 打開 Console 來場邏輯大考驗吧：

// ==========================================
// 練習1：預設值大戰 (?? vs ||)
// 任務：設定史萊姆的攻擊力 (atk)
// ==========================================

/* 💡 情境說明：
   1. 史萊姆 A 的 atk 是 null。
   2. 史萊姆 B 的 atk 是 0 (代表它沒牙齒，攻擊力真的是 0)。
   3. 史萊姆 C 的 atk 是 10。
   
   請定義一個 fallbackAtk = 50，並使用 ?? 設定結果。
*/

/* ✍️ 作答區 */
// 請在此寫出 A, B, C 的正確設定邏輯


// 測試代碼 (預期輸出 A: 50, B: 0, C: 10)
// console.log(`A: ${slimeAAtk}, B: ${slimeBAtk}, C: ${slimeCAtk}`);

// ==========================================
// 練習2：通行許可 (&& 的連鎖)
// 任務：只有「等級 10 以上」且「具備通行證」才顯示內容
// ==========================================

/* 💡 情境說明：
   不要使用 if / else。
*/

/* ✍️ 作答區 */
// const level = 12;
// const hasTicket = true;
// (請在此用一列代碼完成判斷)


// 🚩 總結：
// `&&` 給我看條件，`||` 給我備胎，`??` 才是精準守衛。
// 完成後，將你的邏輯鍊貼至 QuestBoard 吧！
