// ⚔️ Day 09：陣列大戰：全自動化生產線 ⚔️
// 任務說明：請按下鍵盤上的 F12 或右鍵點擊「檢查」，打開 Console 面板觀察你的加工成果。

/**
 * 🛡️ 公會物資處自動化計畫
 * ----------------------------
 * 雖然傳統的 for 迴圈很穩，但面對堆成山的戰利品，我們需要更現代的生產線。
 * 
 * ⚔️ 修煉重點：
 * 1. filter() 篩選：只留下我們要的。
 * 2. map() 加工：改變每個人的樣子，但不改動原始人。
 * 3. Chaining 連鎖連發：高階勇者的串接術。
 * 4. reduce() 歸併：鍊金術般的終極計算。
 */

// ==========================================
// 練習 1：【魔物掃描】過濾危險目標 (filter)
// ----------------------------
/* 
  公會大門口安裝了感應裝置。
  請將 monsters 陣列中「血量 HP 超過 100」的危險魔物篩選出來，存入名為 dangerousMonsters 的新陣列。
*/
const monsters = [
  { name: "哥布林", hp: 30 },
  { name: "地獄犬", hp: 150 },
  { name: "史萊姆", hp: 10 },
  { name: "奇美拉", hp: 200 }
];

/* ✍️ 作答區 */
const dangerousMonsters = []; // 請施展 filter 魔法

console.log("練習 1 - 危險名單：", dangerousMonsters);


// ==========================================
// 練習 2：【等級換算】勇者經驗值轉換 (map)
// ----------------------------
/* 
  公會會根據角色的「等級 (level)」給予「經驗值獎勵」，公式為：level * 10。
  請將 levels 陣列中的純數值，轉換為一個包裝過的描述字串陣列。
  預期產出：["第 1 名：獎勵 10 XP", "第 2 名：獎勵 20 XP", ...]
*/
const levels = [1, 5, 12, 30];

/* ✍️ 作答區 */
const xpRewards = []; // 請施展 map 魔法（提示：記得要 return 喔！）

console.log("練習 2 - 獎勵清單：", xpRewards);


// ==========================================
// 練習 3：【鍊武場】殘次品打折再上架 (Method Chaining)
// ----------------------------
/* 
  有一批武器要拍賣，但規矩如下：
  1. 篩選：剔除掉已經損壞 (isBroken: true) 的武器。
  2. 加工：剩下的武器一律變為「特價：原價 * 0.5」。
  3. 複刻：必須產生一個包含新價格 salePrice 與原物件所有屬性的新名單 (提示：spread ...)。
*/
const shopItems = [
  { name: "生鏽的劍", price: 100, isBroken: true },
  { name: "普通的弓", price: 300, isBroken: false },
  { name: "附魔魔杖", price: 800, isBroken: false }
];

/* ✍️ 作答區 */
const finalSale = []; // 請施展 filter + map 鍊連發

console.log("練習 3 - 清點拍賣會清單：", finalSale);


// ==========================================
// 練習 4：【終極鍊金】計算小隊總戰力 (reduce)
// ----------------------------
/* 
  挑戰題 (Elite Only)：
  請計算以下小隊成員的總共 atk (攻擊力) 數值總合。
*/
const party = [
  { name: "辛梅爾", atk: 120 },
  { name: "芙莉蓮", atk: 550 },
  { name: "艾冉", atk: 280 }
];

/* ✍️ 作答區 */
const totalAtk = 0; // 請施展 reduce 終極鍊金術

console.log(`練習 4 - 🔥 小隊總攻擊力：${totalAtk}`);


// ==========================================
// 🏁 鑑定完畢
// ----------------------------
// 導師的話：當你學會了 map/filter 的鏈式組合，你就已經不再是那個手動搬運資料的普通冒險者了。
// 記住：生產線要保持「純淨」，不要在 map 裡弄髒原始資料！
// 完成後，將你的演武場網址回傳至 QuestBoard 公會報到處吧。
