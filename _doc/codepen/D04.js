// ⚔️ Day 04：實戰演武場 —— 傳奇容器與瞬間開箱術 ⚔️
// 導師：新夥伴，既然看完了講義，就來演武場流點汗吧！
// 任務說明：請利用「物件」、「陣列」與「解構」的招式完成以下挑戰。

// ==========================================
// 練習一：標籤與暗號 (Object Access)
// ==========================================
/* 
   任務：
   1. 使用「點記法」印出寶箱的名稱 (name)。
   2. 使用「變數 secretKey」配上「括弧法」印出寶箱的物資 (content)。
*/

const chest = {
    name: "公會隱藏寶箱",
    content: "傳奇聖劍",
    "required-level": 10
};

const secretKey = "content";

// ✍️ 請在下方實作：
console.log("🛡️ 寶箱名稱：", /* 點記法 */);
console.log("💎 內部物資：", /* 括弧法 */);


// ==========================================
// 練習二：名單管理術 (Array & isArray)
// ==========================================
/* 
   任務：
   1. 先幫導師鑑定，party 是否為一個「陣列」(Array.isArray)。
   2. 隊友「贊恩」加入了隊伍，請將他加入名單最後面 (push)。
   3. 操作完畢後，印出目前隊伍總共有幾個人 (length)。
*/

const party = ["芙莉蓮", "菲倫", "休塔爾克"];

// ✍️ 請在下方實作：
console.log("🔍 鑑定結果：", /* isArray */);


// ==========================================
// 練習三：瞬間取物術 (Destructuring)
// ==========================================
/* 
   任務：
   1. 使用「物件解構」從 hero 中取出 heroName，並「改名」存為 name。
      (提示：有時候公會給的物資標籤太長了，自己取個好記的名字能讓戰鬥更順利！)
   2. 使用「陣列解構」從 party 中取出第一位與第三位夥伴 (跳過第二位)。
*/

const hero = { heroName: "辛梅爾", hp: 100 };
const partyMembers = ["勇者", "僧侶", "戰士"];

// ✍️ 請在下方實作：
// const { ... } = hero;
// console.log(name); // 驗證是否成功改名並取出

// const [ ... ] = partyMembers;


// ==========================================
// 練習四：穿透術 (Nested Destructuring)
// ==========================================
/* 
   任務：精英課挑戰！
   請在一行解構咒語中，直接取出巢狀物件內寶藏的名字 (itemName)。
   🎯 預期輸出：找到傳說中的 [itemName] 了！
*/

const dungeon = {
    chest: {
        content: {
            itemName: "世界地圖"
        }
    }
};

// ✍️ 請在下方實作：
// 提示：左邊的解構層級，要長得跟右邊的資料結構「一模一樣」喔！


// ==========================================
// 演武場結束！恭喜你，離勇者辛梅爾的境界又更近了一步。
// ==========================================
